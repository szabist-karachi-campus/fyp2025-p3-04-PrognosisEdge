<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/eggshellColor">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Back Button -->
            <ImageView
                android:id="@+id/back"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/back"
                android:contentDescription="Back" />

            <!-- Report Title -->
            <TextView
                android:id="@+id/report_title"
                style="@style/orbitron_Heading_Dark"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Analytics"
                android:paddingTop="12dp"
                android:paddingBottom="4dp" />

            <!-- Asset Summary Label -->
            <TextView
                android:id="@+id/asset_summary_title"
                style="@style/proximanova_bold"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Asset Summary"
                android:paddingBottom="4dp" />

            <!-- Summary stats -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="8dp"
                android:paddingBottom="8dp">

                <TextView
                    android:id="@+id/stat_total_readings"
                    style="@style/proximanova_Body_Dark"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Total Readings: " />

                <TextView
                    android:id="@+id/stat_failure"
                    style="@style/proximanova_Body_Dark"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Failures: " />

                <TextView
                    android:id="@+id/stat_no_failure"
                    style="@style/proximanova_Body_Dark"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="No Failures: " />
            </LinearLayout>

            <!-- Failure Type Breakdown Chart -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Failure Types Breakdown"
                android:textStyle="bold"
                android:layout_marginTop="12dp"
                android:textAppearance="@style/proximanova_Body_Dark" />

            <com.github.mikephil.charting.charts.HorizontalBarChart
                android:id="@+id/failure_type_chart"
                android:layout_width="match_parent"
                android:layout_height="180dp"
                android:layout_marginTop="4dp" />

            <!-- Sensor Averages Chart -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sensor Averages"
                android:textStyle="bold"
                android:layout_marginTop="12dp"
                android:textAppearance="@style/proximanova_Body_Dark" />

            <com.github.mikephil.charting.charts.BarChart
                android:id="@+id/avg_values_chart"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:layout_marginTop="4dp" />

            <!-- Add bottom padding to prevent FAB overlap -->
            <View
                android:layout_width="match_parent"
                android:layout_height="120dp" />

        </LinearLayout>
    </ScrollView>

    <!-- Floating Action Button for PDF Export -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_export_pdf"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="90dp"
        android:src="@drawable/ic_download"
        android:contentDescription="Export to PDF"
        app:backgroundTint="#FD6746"
        app:tint="#FFFFFF"
        app:borderWidth="0dp"
        app:elevation="6dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>